// Name = Ubaid 
// Date = 29-Sep-2022

const vehicles = ["Civic", "Honda", "Fortuner", "Audi"];
vehicles.map((vehicle) => {
    console.log(`I would like to own a ${vehicle}`);
    console.log(`I have a ${vehicle}`);
});

vehicles.map((vehicle) => console.log(`I would like to own a ${vehicle}`));

vehicles.map((vehicle) => console.log(`I have a ${vehicle}`));